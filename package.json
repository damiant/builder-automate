{
  "name": "builder-factory",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "agent-native dev",
    "dev:stop-stale": "bash scripts/stop-stale-dev-servers.sh",
    "build": "pnpm -r build",
    "prebuild": "agent-native doctor --strict",
    "doctor": "agent-native doctor",
    "agent-native:doctor": "agent-native doctor",
    "typecheck": "pnpm -r typecheck",
    "repair:workspace-org": "tsx scripts/repair-workspace-org.ts",
    "skills:update": "agent-native skills update scaffold --project",
    "upgrade:agent-native": "agent-native upgrade",
    "fmt:check": "oxfmt --check .",
    "lint": "pnpm fmt:check"
  },
  "dependencies": {
    "@agent-native/core": "0.187.0",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "oxfmt": "catalog:",
    "tsx": "catalog:",
    "typescript-7": "catalog:"
  },
  "packageManager": "pnpm@10.29.1",
  "agent-native": {
    "workspaceCore": "@builder-factory/shared"
  }
}
